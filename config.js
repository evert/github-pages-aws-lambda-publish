// Get a token from https://github.com/settings/tokens
const TOKEN = 'foo';

// What's your username?
const USERNAME = 'evert';

// The repository name
const REPO = 'evert.github.com';

module.exports = {
  TOKEN,
  USERNAME,
  REPO
};
